<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.effill" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Effill"
        android:usesCleartextTraffic="true" >
        <activity android:name=".ExistingCase" >
        </activity>
        <activity android:name=".Contact" />
        <activity android:name=".Payment" />
        <activity android:name=".ChangeView" />
        <activity android:name=".SupremeCourt" />
        <activity android:name=".Navigation_main" />
        <activity android:name=".FileNewCase" />
        <activity android:name=".MainScreen" />
        <activity android:name=".Login" />
        <activity
            android:name=".ui.login.Login"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.login.Logins"
            android:label="@string/title_activity_logins" />
        <activity android:name=".OTP" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>